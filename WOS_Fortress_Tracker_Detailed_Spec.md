# 🚀 WOS 요새쟁탈 명단 PRO (v1.4.2) 시스템 상세서

본 문서는 **1953 GOM 연맹**의 요새전 운영 효율화를 위해 개발된 명단작성 도구의 최종 기술 명세입니다.

---

## 1. 시스템 아키텍처
- **Frontend**: React (Vite) + Tailwind CSS
- **AI Engine**: Google Gemini API (`gemini-2.5-flash`)
- **Backend/DB**: Python Flask + Vercel KV (Redis) / master_list.json (Fallback)

---

## 2. 핵심 기술 명세

### 2.1 AI OCR 엔진 (`gemini-2.5-flash`)
- **모델 확정**: 최신 `gemini-2.5-flash` 모델을 통한 고속/고성능 이미지 분석.
- **할당량 최적화 (v1.4.2)**:
  - **배치 처리(Batch Processing)**: 이미지를 10장씩 묶어 하나의 API 요청으로 처리하여 일일 할당량 소모를 획기적으로 절감.
  - **속도 제어**: 요청 간 2초의 대기 시간을 두어 API 안정성 확보.
- **데이터 정제 및 보정 (Algorithm v8.2)**:
  - **한자 지원**: 한글, 영문뿐만 아니라 한자(`\u4e00-\u9fff`)를 포함한 닉네임 정밀 인식 지원.
  - **공백 무시 매칭**: 영문/한글 사이의 공백 유무와 상관없이 동일인으로 인식하도록 매칭 전 공백 제거 로직 적용.
  - **포함 관계 분석**: 닉네임 일부분만 인식되어도 마스터 명단과 지능적으로 대조 (예: `大五哥` → `大五哥(대오가)` 매칭).
  - **공공 태그 제거**: `[GOM]` 등 연맹 태그 자동 필터링.

### 2.2 실시간 데이터 동기화 (GAS)
- **참여자목록(Master)**: 마스터 명단 데이터 실시간 로드 및 개별 셀 업데이트 지원. (A:이름, B:등급, C:분류 매핑 준수)
- **참여현황(Status)**: 
  - **일괄 처리(Batch Update)**: 전체 데이터를 필터링 후 한꺼번에 쓰는 방식으로 안정성 및 속도 극대화.
- **CORS 대응**: Simple Request 방식을 사용하여 브라우저 정책 준수.

### 2.3 UI/UX 디자인
- **테마**: 설원(Blizzard) 테마 기반의 화이트/블루 톤 적용.
- **모달 시스템**: 현대적인 글래스모피즘(Glassmorphism) 커스텀 모달 적용.
- **버전 관리**: 할당량 및 보정 로직이 개선된 `v1.4.2` 적용.

---

## 3. 기능 명세 히스토리 및 작업 내역

### 3.1 ✅ 완료된 작업 (v1.4.2)
- [x] **할당량 최적화**: 이미지 10장 단위 배치 스캔 적용 (API 호출 횟수 감소)
- [x] **글로벌 닉네임 지원**: 한자(중문) 닉네임 인식 및 보정 로직 강화
- [x] **공백 이슈 해결**: 영문-한글 간 공백 유무와 상관없이 동일인으로 자동 매칭
- [x] **연맹 태그 정제**: 연맹 태그(`[GOM]`) 및 괄호 별칭 정제 로직 고도화
- [x] **모델 안정화**: 명세서 기준인 `gemini-2.5-flash` 모델 고정 사용

---

## 4. 유지보수 가이드
- **API 키**: `.env` 파일의 `VITE_GEMINI_API_KEY`를 통해 관리.
- **GAS 주소**: `App.jsx` 내 `CONFIG.GAS_URL`에 웹 앱 URL 설정.
- **데이터 구조**: 구글 시트의 `참여자목록` 탭 컬럼 순서(이름, 등급, 분류) 엄수.

---
**Created by 판다곰 (1953 GOM Alliance)**
*Technical Support by Antigravity (Google Deepmind)*
*Last Updated: 2026-04-27 (v1.4.2)*
