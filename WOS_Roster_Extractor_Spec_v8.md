# WOS Roster Extractor 기술 상세 명세 (v8)

본 문서는 화이트아웃 서바이벌(WOS) 연맹 명단 추출기의 핵심 로직을 모듈화하여 재구현하거나 프롬프트로 활용하기 위한 명세서입니다.

## 1. 전처리 모듈 (Preprocessing)
OCR로 읽어온 가공되지 않은 닉네임(Raw Name)을 정제하는 단계입니다.

### 1.1 장식 문자 및 기호 제거
- **규칙**: 한글, 영문, 숫자가 시작되기 전의 모든 특수문자와 끝난 뒤의 모든 특수문자를 제거합니다.
- **정규식**:
  - 시작 기호 제거: `^[^a-zA-Z0-9가-힣]+`
  - 끝 기호 제거: `[^a-zA-Z0-9가-힣]+$`
- **효과**: `︶°ʚ천곰ɞ°︶` → `천곰`

### 1.2 연맹 태그 및 한글 우선 추출
- **연맹 태그**: `[GOM]` 등 대괄호로 묶인 태그를 제거합니다.
- **한글 우선 매칭**: 닉네임이 한글 + 공백 + 영문 형태일 경우, 한글 부분(2자 이상)을 우선적으로 추출합니다.
- **효과**: `누나곰 Nuna` → `누나곰`, `난데나야 x` → `난데나야`

## 2. 매칭 엔진 (Matching Engine)
정제된 이름과 마스터 리스트(Master List)를 대조하여 최적의 대상을 찾는 로직입니다.

### 2.1 유사도 알고리즘 (Levenshtein Distance)
- **알고리즘**: 편집 거리 알고리즘을 사용하되, 공백을 완전히 제거한 상태에서 비교합니다.
- **점수 산출**: `(1 - 편집거리 / 두 문자열 중 최대 길이) * 100`

### 2.2 우선순위 필터링
1. **완전 일치**: 공백 제거 후 마스터 이름 혹은 괄호 안 별칭과 100% 일치하면 즉시 반환.
2. **포함 관계**: 정제된 이름이 마스터 이름에 포함되거나, 반대로 마스터 이름이 정제된 이름에 포함되면 90점 부여.
3. **유사도 매칭**: 위 조건에 해당하지 않을 경우 레벤슈타인 점수가 가장 높은 대상을 선택.
- **임계값(Threshold)**: 최종 점수가 50점 미만일 경우 '매칭 실패'로 간주하여 오매칭을 방지합니다.

## 3. 병렬 처리 아키텍처 (Parallel Processing)
대량의 이미지를 5초 이내에 처리하기 위한 구조입니다.

### 3.1 동시성 제어
- **방식**: `for...of` 대신 `Promise.all()`을 사용하여 모든 이미지에 대한 API 요청을 병렬로 전송합니다.
- **흐름**:
  1. 이미지 파일 배열 확보
  2. `map()` 함수를 통해 각 이미지별 `analyzeSingleImage` 비동기 함수 호출
  3. 모든 API 응답이 도착할 때까지 대기 (가장 느린 한 장의 응답 시간이 전체 시간이 됨)
  4. 결과 병합 및 중복 제거(Set)

## 4. AI 프롬프트 가이드라인 (System Prompt)
Gemini 2.5 Flash 모델에 전달할 최적의 지시문입니다.

**System Instruction**:
> "너는 WOS 게임의 연맹 관리자이다. 업로드된 스크린샷에서 모든 유저의 닉네임을 추출하라.
> 오직 닉네임만 추출할 것.
> 중복된 이름은 하나로 합칠 것.
> 응답은 반드시 JSON Array [ "이름1", "이름2", ... ] 형식으로만 출력할 것."

## 5. 코드 구현 가이드 (JavaScript Context)
핵심 매칭 함수 구조입니다.

```javascript
function findBestMatch(raw, masterList) {
    const cleaned = cleanRawName(raw); // 1번 모듈 적용
    let best = { name: "매칭 실패", similarity: 0 };

    for (const member of masterList) {
        let score = calculateScore(cleaned, member); // 2번 모듈 적용
        if (score > best.similarity) {
            best = { ...member, similarity: score };
        }
    }
    return (best.similarity >= 50) ? best : { name: "매칭 실패", similarity: 0 };
}
```
