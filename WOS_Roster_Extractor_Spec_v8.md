# [⚠️ 중요] 모델 버전 고수 원칙
> **모델은 절대 1.5 버전으로 교체해서는 안 됩니다.** 반드시 **2.5 버전 이상**을 유지해야 하며, 현재 권장 모델은 **Gemini 2.5 Flash** 이상입니다. 모델 성능 저하 방지를 위해 이 규칙은 최우선적으로 준수되어야 합니다.

# WOS Roster Extractor 기술 상세 명세 (v8.2)

본 문서는 화이트아웃 서바이벌(WOS) 연맹 명단 추출기의 핵심 로직을 모듈화하여 재구현하거나 프롬프트로 활용하기 위한 명세서입니다.

## 1. 전처리 모듈 (Preprocessing)
OCR로 읽어온 가공되지 않은 닉네임(Raw Name)을 정제하는 단계입니다.

### 1.1 장식 문자 및 기호 제거
- **규칙**: 한글, 영문, 숫자, **한자**가 시작되기 전의 모든 특수문자와 끝난 뒤의 모든 특수문자를 제거합니다.
- **범위**: 한글(`가-힣`), 영문(`a-zA-Z`), 숫자(`0-9`), **한자(`\u4e00-\u9fff`)**
- **효과**: `︶°ʚ大五哥ɞ°︶` → `大五哥`

### 1.2 연맹 태그 및 괄호 제거
- **연맹 태그**: `[GOM]` 등 대괄호로 묶인 태그를 제거합니다.
- **별칭 제거**: 닉네임 뒤에 붙은 `(별칭)` 등 소괄호 내용을 제거하여 순수 닉네임만 확보합니다.
- **효과**: `[GOM]누나곰(Nuna)` → `누나곰`

## 2. 매칭 엔진 (Matching Engine)
정제된 이름과 마스터 리스트(Master List)를 대조하여 최적의 대상을 찾는 로직입니다.

### 2.1 유사도 알고리즘 (Space-Insensitive Matching)
- **알고리즘**: 비교 직전 모든 공백과 특수문자를 제거하고 대문자로 통일하여 비교합니다.
- **효과**: `Queen 꽐쪽이` vs `Queen꽐쪽이` → 모두 `QUEEN꽐쪽이`로 변환되어 **100% 일치** 판정.

### 2.2 우선순위 필터링
1. **완전 일치**: 공백 제거 후 마스터 이름 혹은 괄호 안 별칭과 100% 일치하면 즉시 반환.
2. **포함 관계**: 정제된 이름이 마스터 이름에 포함되거나, 반대로 마스터 이름이 정제된 이름에 포함되면 90점 부여. (예: `大五哥` vs `大五哥(대오가)`)
3. **유사도 매칭**: 레벤슈타인 점수가 50점 이상인 대상을 선택.

## 3. 할당량 최적화 아키텍처 (Quota Management)
API 호출 횟수를 줄이고 안정성을 높이기 위한 구조입니다.

### 3.1 배치 처리 (Batching)
- **방식**: 이미지를 1장씩 처리하는 대신, **10장씩 묶어서(Batch size: 10)** 하나의 API 요청으로 전송합니다.
- **효과**: 동일 이미지 수 대비 API 호출 횟수를 **1/10 수준으로 절감**하여 할당량 소모를 최소화합니다.

### 3.2 속도 제어 (Rate Limiting)
- 배치 간 최소 2.0초의 대기 시간(Sleep)을 두어 429(Too Many Requests) 에러를 방지합니다.

## 4. AI 프롬프트 가이드라인 (System Prompt)
### 권장 모델: Gemini 2.5 Flash 이상 (1.5 사용 절대 금지)

**System Instruction**:
> "너는 WOS 게임의 연맹 관리자이다. 제공된 여러 장의 스크린샷을 모두 꼼꼼하게 분석하라.
> 각 스크린샷에 등장하는 모든 유저의 닉네임을 하나도 빠뜨리지 말고 추출해야 한다.
> 중복된 이름은 최종 결과에서 하나로 합치되, 모든 이미지의 데이터를 전수 조사하라.
> 응답은 반드시 JSON Array [ "이름1", "이름2", ... ] 형식으로만 출력할 것."

## 5. 핵심 코드 구현 구조 (Pseudo Code)

```javascript
// 전처리: 한자 포함 및 태그 제거
const cleanName = (n) => n.replace(/\[.*?\]|\(.*?\)/g, "").replace(/^[^a-zA-Z0-9가-힣\u4e00-\u9fff]+|[^a-zA-Z0-9가-힣\u4e00-\u9fff]+$/g, "").trim();

// 매칭: 공백 제거 후 비교
const calculateScore = (raw, master) => {
    const r = raw.replace(/[^a-zA-Z0-9가-힣\u4e00-\u9fff]/g, "").toUpperCase();
    const m = master.replace(/[^a-zA-Z0-9가-힣\u4e00-\u9fff]/g, "").toUpperCase();
    if (r === m) return 100;
    if (r.includes(m) || m.includes(r)) return 90;
    return levenshtein(r, m);
};
```
